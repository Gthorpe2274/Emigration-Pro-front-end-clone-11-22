[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Proxy API requests to Cloudflare Workers backend
[[redirects]]
  from = "/api/*"
  to = "https://emigration-pro.aiservices4biz.workers.dev/api/:splat"
  status = 200
  force = true

# SPA redirect rule - all non-API routes should be handled by React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200